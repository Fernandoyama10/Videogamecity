<%- include layouts/header_regis.ejs %>
<header>
<div class="banner">
    <div class="">
        <div class="row fill_height">
    
            <div class="col-lg-12">
                <div class="global-container">
                    <div class="card login-form">
                    <div class="card-body">
                        <h3 class="card-title text-center">Iniciar Sesión</h3>
                        <div class="text-center"><img style="height:70px;width:70px" src="/imagenes/estrella.png" alt=""></div>
                        
                        <div class="card-text">
                            <!--
                            <div class="alert alert-danger alert-dismissible fade show" role="alert">Incorrect username or password.</div> -->
                            <% if(typeof message!= "undefined" && message) { %>
                                <div class="alert alert-danger alert-dismissible fade show" role="alert"><%= message %></div>

                            <% } %>
                            <% if(typeof messageregister!= "undefined" && messageregister) { %>
                                <div class="alert alert-success alert-dismissible fade show" role="alert"><%= messageregister %></div>

                            <% } %>
                            <div class="form-s">
                                <!-- to error: add class "has-danger" -->

                                <form action="/auth/login" method="POST">
                                    <div class="form-group">
                                        <label for="exampleInputEmail1"></label>
                                        <input type="email" class="form-control form-control-sm" placeholder="Ingresa tu correo" id="correo" name="correo" maxlength = "100" required >
                                    </div>
                                    <div class="form-group">
                                        <label for="exampleInputPassword1"></label>
                                       
                                        <input type="password" class="form-control form-control-sm" placeholder="Contraseña" id=contrasena"" minlength="5" maxlength = "40" name="contrasena" required >
                                    </div>
                                    <br>
                                    <br>
                                    <button type="submit" class="btn btn-primary btn-block">Login</button>
                                </form>
                                    <br>
                                    <br>
                                    <div class="sign-up">
                                        ¿No tienes cuenta? <a href="/registro">Registrate</a>
                                    </div>
                           

                            
                            </div>
                        </div>
                    </div>
                </div>
                </div>
                
            </div>
        </div>
    </div>
</div>
</header>
<%- include layouts/footer.ejs %>